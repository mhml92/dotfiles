#!usr/bin/python3

import os
DEBUG = True

def call(command):
	if DEBUG:
		print(command)
	else:
		os.system(command)

################################################################################
## init
call("git submodule update --init --recursive")
################################################################################

################################################################################
# dependencies
dep = [
	'stow',
	'git',
	'vim',
	'cmake',
	'python-dev',
	'python3-dev',
	'build-essential'
]

call('apt-get install ' +  " ".join(dep))
################################################################################

################################################################################
# install gruvbox terminal theme
call("wget -O xt  http://git.io/v6JYg && chmod +x xt && ./xt && rm xt")

################################################################################

################################################################################
stow = ['vim', 'ranger']
################################################################################


